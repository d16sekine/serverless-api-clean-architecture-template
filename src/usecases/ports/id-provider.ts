// ------------------------------------------------------------------
// port（ID 生成の抽象）
// 「ID をどう作るか」は技術詳細。usecase はこの抽象にだけ依存し、
// 実装（UUID など）は infrastructure 層に置く。テストではフェイクを注入できる。
// ------------------------------------------------------------------

export type IdProvider = {
  generate(): string
}
