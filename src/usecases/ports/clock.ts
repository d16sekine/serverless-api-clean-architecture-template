// ------------------------------------------------------------------
// port（時刻取得の抽象）
// 現在時刻の取得は副作用のある技術詳細。抽象化しておくことで、
// usecase / domain を決定的にテストできる（new Date() のモックが不要になる）。
// ------------------------------------------------------------------

export type Clock = {
  now(): string // ISO 8601 形式の文字列
}
